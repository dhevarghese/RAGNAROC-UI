files:
    "/etc/cron.d/mycron":
        mode: "000644"
        owner: root
        group: root
        content: |
            0 2 * * * root rm -r /var/app/current/file_system_store  

commands:
    remove_old_cron:
        command: "rm -f /etc/cron.d/mycron.bak"